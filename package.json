{
	"name": "local-markdown-flashcard-app",
	"version": "0.1.0",
	"private": true,
	"description": "Local-first flashcard app backed by a single Markdown file.",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "node --watch server.js",
		"test": "node --test"
	},
	"engines": {
		"node": ">=18"
	},
	"dependencies": {
		"express": "^4.21.2",
		"js-yaml": "^4.1.0"
	}
}
